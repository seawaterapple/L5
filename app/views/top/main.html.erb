<% @tweets.each do |e| %>
    <%= e.message %>
    <%= link_to 'いいね', likes_path(tweet_id: e.id), method: :post %>
    <%= link_to 'いいね削除', like_path(e), method: :delete %>
<% end %>

<%= link_to '戻る', :back %>

<h1>ログイン中</h2>
<%= link_to "ログアウト", session[:login_uid] = nil %>